# Supabase project settings — Project Settings → API in your Supabase dashboard.
NEXT_PUBLIC_SUPABASE_URL=
NEXT_PUBLIC_SUPABASE_ANON_KEY=

# Server-only — used only in server contexts that intentionally bypass RLS
# (none in V1). Never prefix this with NEXT_PUBLIC_, never import it in a
# Client Component, never commit a real value.
# SUPABASE_SERVICE_ROLE_KEY=
